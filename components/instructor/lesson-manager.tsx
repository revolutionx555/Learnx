export default function LessonManager({ courseId, instructorId }) {
  // This can later be expanded to full CRUD for lessons
  return (
    <div className="p-4 bg-slate-100 rounded">
      <p>Lesson management features coming soon! (Course ID: {courseId})</p>
    </div>
  );
}